---
import AuiIcon from "./react/AuiIcon.tsx";

interface Props {
  name: string;
  size?: number;
  class?: string;
}

const { name, size = 18, class: className = "" } = Astro.props;
---

<AuiIcon client:load name={name} size={size} className={className} />
