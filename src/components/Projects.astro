---
import Container from "./Container.astro";
import SectionEyebrow from "./SectionEyebrow.astro";
import portfolio from "../data/portfolio.json";
import BlurFade from "./react/BlurFade.tsx";
import ProjectCarousel from "./react/ProjectCarousel.tsx";

const { projects } = portfolio;
---

<section id="projects" class="py-14 sm:py-20" style="border-top: 1px solid var(--border);">
  <Container>
    <BlurFade client:visible>
      <SectionEyebrow icon="blocks">// projects</SectionEyebrow>
      <h2 class="mb-10 text-2xl font-extrabold sm:text-3xl" style="color: var(--text);">
        Stuff I've shipped
      </h2>
    </BlurFade>
  </Container>

  <div class="mx-auto mt-0 w-full max-w-3xl px-5 sm:px-6">
    <ProjectCarousel client:only="react" projects={projects}>
      <div class="h-[16.5rem] rounded-xl"></div>
    </ProjectCarousel>
  </div>
</section>
